# ec2discord-tcpclient

```
% ./client --help
Usage of client:
  -host string
    	Specify destination address (default "localhost")
  -port string
    	Specify destination port (default "9002")
  -proto string
    	Specify protocol (tcp/udp) (default "tcp")
```
